# 小优真实数字员工闭环 V1 生产灰度记录

日期：2026-08-13  
底座：Hermes v0.20.0 + Agnes 2.5 Flash  
代码：`9f71eae`、`386c36b`

## 一、上线结论

真实数字员工闭环 V1 已完成本地实现、对抗审查和第一批生产部署。当前只开放金总 `JinWenJie` 与李老师测试号 `CeShi`，店长、其他老师和家长均未开放主动触达。

本次上线建立了正式主动授权、候选执行、发送回执、目标行动、回复证据、低风险目标子任务、未回复升级、撤销停止和诚实表达守卫。系统仍只提供权限、证据、幂等、审计、写后反查、频率和外发边界，具体行动由小优主模型判断。

## 二、本地门禁

- 固定回归：`243 passed`。
- 真实员工闭环对抗测试：`12 passed`。
- 语法检查、差异检查和敏感扫描：通过。
- demo tenant 初始化与验收：`STATUS:PASS`，只有学生导入为空的预期 warning。
- Git 远程基线：`origin/main` 已包含 `9f71eae` 和 `386c36b`。

## 三、生产部署

- 备份目录：`/opt/hermes-youyi-upgrade-0.20.0/backups/real-employee-v1-9f71eae-20260813T173016/`。
- 同步范围：11 个 `tuoguan_core` 文件、核心 Skill、历史状态修复脚本和生产加载检查脚本。
- 同步位置：runtime、site-packages lib、site-packages lib64、Home 插件副本及 Home Skill。
- 严格加载检查：四份插件哈希一致，核心模块可导入，104 个托管工具完成注册，6 个新增接口可见。
- 主服务：`active`，监听 `127.0.0.1:8866`。
- 定时器：自主唤醒、早晚报、outbox、存储巡检、社交市场和外部学习均已恢复。
- 错误日志：部署后未发现 `Traceback`、`ImportError`、`ModuleNotFoundError`、`ERROR` 或 `Exception`。
- outbox：`sent=90`、`suppressed=22`、`superseded=1`，没有 `pending/retry_pending/sending/result_unknown`。

部署过程中有两次安装前自检因 systemd 停止态表现和空 PID 检查方式不兼容而自动中止。两次均发生在代码和数据修改前，旧服务与定时器自动恢复；修正部署检查后才执行正式备份、同步和启动。

## 四、历史状态收口

- 旧的李老师未发送主动候选已标记为 `superseded`，没有补发。
- 错误保存为语气偏好的“自主决定找谁”已标记为语义失配。
- 已建立两条正式主动授权：金总和李老师测试号，每人每天最多 2 条。
- 既有活动目标已补一条 `query_internal_data` 行动，用于先核对事实基线、证据和责任关系。
- 该历史行动不主动外发、不直接派任务，避免把旧学生名单或旧责任关系当成当前事实。

## 五、当前灰度门禁

代码上线不等于业务闭环已经完成。第一批仍需在自然工作节奏中完成三条真实链路：

`小优判断需要取数 -> 向李老师发送 -> 李老师回复 -> 小优判断证据完整度 -> 更新目标行动 -> 验证结果`

连续观察至少 48 小时且没有错人、重复发送、虚假成功、无法取消、跨人记忆或未经授权任务后，才评估开放店长 `CuiXiaoXia`。店长通过两条运营事实闭环和后续 48 小时观察后，才评估开放其他已批准在职老师。

家长主动触达、工资、绩效、处罚、制度、权限、数据删除和安全最终闭环仍禁止自动执行。

## 六、回滚点

出现服务启动失败、插件失败、重复回复、跨人记忆、任务覆盖、未经授权外发、日报失效、自主循环假成功或任务无法取消时，停止本批灰度，使用上述备份恢复代码和业务账本，并保留故障现场和审计记录。
